//Vieja version de hacer clases

function account( name, document, id, email, password){
    this.id = id
    this.name = name
    this.document = document
    this.email = email
    this.password = password
}